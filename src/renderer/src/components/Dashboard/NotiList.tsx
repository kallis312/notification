import { Box, VStack } from "@chakra-ui/react"

const NotiList = () => {
  return (
    <VStack w={'full'} px={2}>
      <Box w={'full'} rounded={'md'} p={1} borderColor={'teal'} borderWidth={1}>
        asdfasfd
      </Box>
    </VStack>
  )
}

export default NotiList